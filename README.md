# Pocket Pets

A tiny browser game where you guess a secret hiding spot and collect a random round pet.

## Play locally

Open `index.html` in a browser, or run any static web server in this folder.

The pet collection is saved in the browser with `localStorage`.
